{
  "name": "snake-evo",
  "version": "1.0.0",
  "description": "Snake Evo - evolve through 50 real snake species",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed - pure HTML/JS'",
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
